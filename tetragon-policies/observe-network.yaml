# tetragon-policies/observe-network.yaml
# Logs every outbound TCP connection attempt from any pod
apiVersion: cilium.io/v1alpha1
kind: TracingPolicy
metadata:
  name: observe-network
spec:
  kprobes:
    - call: "tcp_connect"
      syscall: false
      args:
        - index: 0
          type: "sock"
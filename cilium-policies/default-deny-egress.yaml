# cilium-policies/default-deny-egress.yaml
# Blocks all outbound connections from demo-app pods
apiVersion: cilium.io/v2
kind: CiliumNetworkPolicy
metadata:
  name: default-deny-egress
  namespace: demo-app
spec:
  endpointSelector: {}
  egress: []             # empty list = deny all egress